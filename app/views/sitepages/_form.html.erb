<%= form_for(@sitepage) do |f| %>
  <% if @sitepage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sitepage.errors.count, "error") %> prohibited this sitepage from being saved:</h2>

      <ul>
      <% @sitepage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :maillink %><br />
    <%= f.text_field :maillink %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br />
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
